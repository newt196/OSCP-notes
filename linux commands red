Linux commands

Finding something that needs to be exported. txt for right now
Command 
tac=reverses the line order.
less=allows you to scroll through the file.
strings=extracts printable characters.
grep .=prints all non-empty lines.
cp to=/dev/stdout prints the entire file.
cat=prints the entire file as-is.
